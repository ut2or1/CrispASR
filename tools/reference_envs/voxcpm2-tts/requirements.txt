# Reference-dump env for `--backend voxcpm2-tts`
# Scaffold generated by tools/audit_diff_coverage.py.
# Edit to add git-only deps and pin versions, then commit.
# Bootstrap: tools/bootstrap_ref_env.sh voxcpm2-tts

numpy>=2.0
# Always required by the dump driver itself
gguf>=0.10

torch

# TODO: install upstream voxcpm from git (pin sha matching the GGUF weights you'll diff against)
# e.g.  voxcpm @ git+https://github.com/openbmb/VoxCPM.git@<sha>
