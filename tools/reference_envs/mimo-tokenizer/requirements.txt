# Reference-dump env for `--backend mimo-tokenizer`
# Scaffold generated by tools/audit_diff_coverage.py.
# Edit to add git-only deps and pin versions, then commit.
# Bootstrap: tools/bootstrap_ref_env.sh mimo-tokenizer

numpy>=2.0
# Always required by the dump driver itself
gguf>=0.10

torch
torchaudio

# TODO: install upstream mimo_audio from git (pin sha matching the GGUF weights you'll diff against)
# e.g.  mimo_audio @ git+https://github.com/XiaomiMiMo/mimo-audio.git@<sha>
