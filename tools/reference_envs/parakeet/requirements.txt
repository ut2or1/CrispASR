# Reference-dump env for `--backend parakeet`
# Bootstrap: tools/bootstrap_ref_env.sh parakeet
#
# Hand-curated. NeMo Parakeet-TDT/CTC FastConformer reference dumper.
# Model is fetched from HF on first run (e.g. nvidia/parakeet-tdt_ctc-0.6b-ja);
# subsequent runs use the ~/.cache/huggingface cache.

numpy>=2.0
gguf>=0.10

# Core deep-learning stack
torch>=2.5

# NeMo is the heavy dep — pulls in PyTorch Lightning, hydra, omegaconf,
# nv_one_logger, and a long tail of OmegaConf/hydra schemas. The
# parakeet ref backend has a hand-patched `overrides` shim to dodge a
# break introduced in overrides 7.x (see top of parakeet.py); pin
# overrides<7 to avoid needing the shim entirely.
nemo_toolkit[asr]>=2.0
overrides<7
