# LEARNINGS.md — index

GENERATED by `tools/gen-learnings-index.py`. Do not hand-edit; re-run it.

`LEARNINGS.md` is ~800 KB and nobody reads it whole. Scan this index, then jump:

```bash
grep -n "<heading text>" LEARNINGS.md   # then Read with that offset
```

Every heading states its lesson in full, so **the index is the summary** — if a
line does not sound relevant, neither is the section under it. `L<n>` is the line
in LEARNINGS.md at generation time; re-run the tool if the numbers look off.

Roughly half the file is transferable method (the topic groups) and half is
per-backend porting detail (Per-model port notes + the family cross-reference).
Reach for the topic groups when you are stuck on a *technique*, and the
cross-reference when you already know which model you are touching.

## Index by topic (276 lessons)

**Security & untrusted input** (2)

- L1373 — Two more untrusted-input lessons: an HTTP server reads the body before your auth check, and a parser that abort()s on malformed input is a DoS a fuzzer finds in seconds
- L1400 — Every hand-rolled file parser sizes a buffer from an untrusted length field — that is the #1 memory-safety bug class, and a multi-agent audit finds them fast

**Compliance, provenance & marking (EU AI Act)** (7)

- L116 — A watermarked TTS output cannot be A/B'd with `cmp` — and whisper LID is not an accent metric
- L676 — Moving a release tag is CLEAN when the fix touches only release TOOLING — the test is provenance, not "never move a tag"
- L10748 — Always rebuild the box-under-test before benchmarking
- L15837 — EU AI Act audit — provenance not suffix
- L15895 — EU AI Act — a PRESET voice can be a real person
- L16345 — consent record — BIND to the audio, don't chain the log
- L17640 — watermark detector — swap the STATISTIC, not the threshold

**Build, CI, release & packaging** (21)

- L20 — A packaging step that destroys the state a later step reads — and scripts that only run in a release job
- L324 — A guard job that runs ONE compiler family guards one compiler family
- L612 — A green release job is not a shipped artifact — verify what you DELIVER, not what the workflow says
- L741 — "Linked in CMake" is NOT evidence the code SHIPS — the linker drops an object nothing references
- L961 — Path-filtered CI workflows ROT — the first re-triggering change inherits ALL the debt accumulated since the last run
- L981 — Scoped `ctest -R` on a PARTIALLY BUILT tree has two name-collision traps: configure-time add_test and catch_discover NOT_BUILT placeholders
- L1266 — Porting a libllama-hosted reference onto our in-house Qwen3 was clean on first contact; and Kaggle `kernels_logs()` is the escape hatch when the output is page-capped
- L1324 — ggml disables CUDA graphs below Ampere (cc < 800) — an Ampere-only re-warm bug can't be reproduced on Kaggle's P100/T4
- L1623 — Two concurrent HuggingFace uploads from one machine cause spurious mid-batch failures — serialize them (#192 aligner re-ship)
- L2092 — Never benchmark on a near-full disk — SIGBUS + nondeterminism masquerade as a code signal (§192)
- L2168 — Kaggle: datasets are per-account, and the ccache dataset has a required shape (§213)
- L2590 — Kaggle regression kernels: write output incrementally, crash-guard everything
- L2717 — Regenerate Go cgo LDFLAGS from a *linux-equivalent* config, never macOS
- L2732 — cmake-js defaults its build dir to `build/` — point it elsewhere (§166)
- L2886 — HF Space is a separate, FLAT repo that silently drifts from `hf-space/`
- L4189 — Windows / MSVC portability
- L5427 — GitHub Actions workflow triggers — `master` → `main` rename gotcha
- L5573 — libcrispasr.a + libcommon.a both define stb_vorbis / miniaudio impl (Linux ld dies)
- L10812 — Kaggle as a batch-rebake target: seven fragilities the script has to work around
- L14256 — CI has no NVIDIA GPU — a CUDA-only graph path (mimo RVQ) is guarded by an on-Kaggle exact-parity smoke, not CI (#309)
- L15083 — #313 Rust crates on crates.io via git dep

**Multi-surface wiring — CLI / C ABI / bindings / server** (24)

- L245 — A gate that ANDs "both sides known" is only as alive as its weakest detector
- L484 — A new REQUIRED request field is a breaking API change on a schedule you don't control — deny the sub-feature, don't refuse the request
- L572 — A hardcoded decode cap silently ignores --max-new-tokens — and forwarding it naively SHRINKS a backend
- L649 — A capability flag is a PROMISE the backend must keep — an unimplemented cap disables the safety nets keyed off it
- L943 — Two writers on one output field at different pipeline stages = an ordering bug — fix the ORDER, don't bolt on provenance
- L1002 — A binding wrapper that has never been EXECUTED is untested code — and parallel agents produce cross-cutting conflicts only the verifier can see
- L1592 — A persistent KV cache reused from a larger allocation makes local `max_ctx` the wrong stride (#171 VibeVoice server leak)
- L2499 — Backend-name guards must match the *registered* name, by prefix when aliased (#171, #174)
- L2633 — A feature has ~8 front-ends — wiring it into one isn't "done" (§166)
- L2659 — A dry-run "preview" must mirror the real resolver, or it lies (§166)
- L2744 — A WebSocket server that "works in the browser" can still be RFC-broken (§166)
- L2758 — Server warmup is the launch-time differentiator vs the CLI (#165)
- L2771 — Don't throw on the per-request server path; surface the real error (#165)
- L6098 — Chatterbox CAMPPlus phase 1 — Kaldi fbank front-end
- L8640 — voxcpm2 VAE — Python wrapper that captures kwargs without forwarding
- L13176 — 2026-07-02 — `--gpu-backend` was silently ignored (#214)
- L13864 — Before starting a "top-priority, profiled" perf target: check whether it's already been tried-and-rejected, and whether a parallel session owns it
- L14345 — A public API with no caller is a harness-blind zone announcing itself — and an ONNX export may FOLD a BatchNorm your C++ still expects as tensors
- L15017 — #311 --strict-pipeline SHIPPED
- L15160 — #315 vibevoice long-form ASR budget: PR fixed only session; CLI+server needed the adapter…
- L15196 — #324 server diarize DROPPED 59% of the transcript
- L17346 — Parakeet #208 long-audio + enc-cache collapse
- L17453 — session-state fixes + moonshine CPU-only SOLVED
- L17677 — Wyoming TTS marked nothing — FIXED 8f4197ec

**Parity, the diff harness & ground truth** (36)

- L289 — The diff harness starts where its INPUT starts — check what it is fed before trusting parity
- L516 — When a clone/zero-shot path misbehaves but the BAKED/preset path is clean, diff the two paths' DATA byte-for-byte — the difference IS the bug
- L779 — Cosine, correlation and peak-match are ALL scale-invariant — a uniform gain error passes every one of them
- L847 — A logits-level diff harness at cos 1.000000 says nothing about the TABLE that turns logits into labels
- L922 — When you can't run the acceptance roundtrip, ship the path gated default-OFF + a provable-equivalence argument — don't block on the model
- L1019 — Deterministic DROPOUT of whole words/tokens (not garble) = a zeroed weight BLOCK in the shipped GGUF — scan converted tensors for zero-norm rows BEFORE debugging the port
- L1064 — A TTS AR model that "never stops" is often a WRONG-SAMPLING-PARAMS bug, not a stop-head/port bug — read the model card's generation params
- L1359 — Validating a TTS perf change by WAV correlation is invalid — melotts `--seed` isn't deterministic; use ASR round-trip or a deterministic-ASR bit-check
- L1441 — MelsTime mel layout IS ggml Conv1d input layout — transposing corrupts with cos ~0.3, not a gradual drift
- L1486 — Baked mel filterbank layout MUST match core_mel's `fb_layout` — a transpose produces cos_min ≈ −0.15, not a gradual drift
- L1689 — A production default that diverges from the upstream reference is a whole class of bug the stage-cosine diff can't see (#192 TADA)
- L1729 — A forced aligner is not an ASR model — its free CTC argmax is all-blank (#192)
- L1738 — When the engine and the PyTorch reference produce the SAME wrong output, the engine is right — the harness input (usually the prompt) is the bug (moss-transcribe)
- L1763 — An input feature the checkpoint wasn't trained with is worse than nothing — the moss-transcribe time-marker experiment (#218)
- L1788 — Stage the PyTorch reference load — encoder and LM as separate standalone modules, never the monolithic `*ForCausalLM` (moss-transcribe)
- L1808 — Parakeet single-pass DROPS whole sections of long audio — chunk-and-merge, and pick the reference clip carefully (#208b)
- L2104 — Always A/B against the *upstream reference*, not just your own option matrix (§216)
- L2155 — TTS WAV md5 is only a valid parity gate with a pinned seed
- L2576 — Regression transcripts need WER tolerance, not byte-exact match (#92)
- L3586 — Methodical debugging of ported models against ground truth
- L5693 — Chatterbox base T3 sampler parity — Gumbel-max → torch.multinomial
- L5732 — Chatterbox HiFT vocoder parity nits
- L8763 — Diff-harness "drift" is mostly the GGUF quant, not a code bug
- L12038 — Pocket TTS — 12 bugs from stub to cos=0.999997
- L13453 — ASR roundtrip does not validate audio ONSETS; reproduce with the original pipeline before debugging your runtime
- L13497 — Porting the missing sub-model is necessary but not sufficient — hunt the hardcoded "unconditional" defaults that ignore it
- L13570 — Python reference dumpers must apply identical audio conditioning as the C++ runtime
- L13587 — A `frames/token` (or chars/sec) length heuristic silently truncates diffusion TTS — run the duration predictor the model already ships
- L13692 — A faithful port can still loop: get the bf16 blueprint's behaviour on the SAME audio before hunting a runtime bug
- L13752 — Generation-config defaults are part of the blueprint contract — a masked-iterative TTS with the wrong sampler knobs degenerates to SILENCE, not to bad audio
- L13896 — CosyVoice3 CFM Euler steps: 6 is the sweet spot, and validate it with log-mel corr vs full-steps — NOT ASR roundtrip
- L14088 — Exact greedy code-parity is unachievable for a QUANTIZED AR audio LM — confirm the port with a step-0 logit-rank probe, not byte-exact codes
- L14138 — A GPU-vs-reference RTF gap that survives kernel parity is per-step HOST detours — fuse them into the step graph; unified-memory Metal hides exactly this class of cost
- L14231 — A normalization stage amplifies upstream float error — input-align it before trusting its cos (§248 MBR)
- L15369 — #333 madlad400 quants + T5 parity
- L16241 — chatterbox hift_pcm(ref_mel) cos≈0.879 was a diff-harness layout bug (source_stft fed transposed), not a…

**ggml graphs, allocation & caching** (43)

- L555 — Reusing a cached scheduler graph across `sched_reset`/`alloc` cycles is CPU-safe but SIGSEGVs on GPU — the reused input tensor is bound to the prior cycle's freed buffer
- L1125 — Metal-validated GPU TTS graph paths DID transfer cleanly to CUDA — the stricter-CUDA risk is real but not universal
- L1161 — BatchNorm-fold must respect the conv tensor's dtype — and a per-backend fix usually has siblings
- L1195 — "Migrate host KV to device-resident" is a ~1% win on a compute-bound transformer decoder — measure the fraction before the rewrite
- L1495 — Never cache a ggml compute graph across calls that share a scheduler with a larger graph — the allocator regrows and frees the cached graph's GPU buffers
- L1506 — Flow-matching DiT with JointAttention REQUIRES attention masking even for unconditional generation — skipping masked KV tokens silently corrupts output (2026-07)
- L1841 — A cached cgraph is NOT re-entrant with ggml_backend_sched — the 2nd reuse silently corrupts (#208)
- L1883 — On Metal a cached decode graph is dispatch-bound, not alloc-bound — measure the step's parts, don't trust wall time (§210, PR #207)
- L2040 — Precomputed attention masks in a GGUF are dead weight if the runtime rebuilds them (§192)
- L2208 — Cross-attention KV is a free F16 win — encoder-decoder backends (§176i)
- L2236 — Localizing a GPU miscompute with CPU-vs-Metal diffing — the ggml_backend_sched weight-less-first-op trap (§206)
- L2390 — A byte-identical standalone reproducer is the only way to (dis)prove a "ggml bug" (§203)
- L2420 — In-place device-KV decode graphs on Metal: three hazards (§176b+c, §203)
- L2528 — Never pass `ggml_graph_get_tensor` directly to `ggml_backend_tensor_set` (#164)
- L2548 — `rope_theta=0` means "no positional encoding" — skip RoPE entirely (#164)
- L2557 — `ggml_siglu` ≠ PyTorch `glu` — the gate/value halves are SWAPPED (#81)
- L2626 — Asymmetric rel-pos shift uses the same formula as symmetric (#81)
- L2671 — Graph input tensors that nothing consumes are invisible to `ggml_graph_get_tensor` (#164)
- L2704 — `ggml_backend_cpu_set_n_threads` asserts CPU — guard it after `init_best()`
- L2817 — Decompose ConvTranspose1d → mul_mat + col2im_1d; port col2im per-backend (#155)
- L2857 — Beam-search KV snapshots must stay on-device (#161)
- L3140 — ggml / inference engine
- L3712 — ggml graph allocation: gallocr vs compute_with_ctx
- L5060 — 2026-04-22 - No-gpu mode must gate `ggml_backend_load_all()`
- L7165 — 2026-05-05 — ggml fork patches we carry (must re-apply on every ggml bump)
- L8436 — voxcpm2 perf — per-step ggml graphs, Metal, SIMD layouts
- L8720 — ggml broadcast hides size-mismatch bugs
- L10940 — ggml scheduler tightened cross-backend tensor resolution between §56 and 2026-05-26 (PLAN #115)
- L12194 — Pocket TTS — manual CPU → ggml compute graph rewrite
- L12433 — AudioSeal ggml port — ggml_pad_ext convention trap
- L12510 — GPU weight mirrors for mixed legacy / graph codebases
- L12634 — ggml_graph_get_tensor hash invalidated by ggml_gallocr
- L12827 — read_tensor_f32 weight pre-cache for VITS-family TTS
- L12840 — Single-token embed graph elimination for LLM-ASR
- L13354 — A once-allocated-then-reused bucket step graph is a CUDA-graph-capture hazard: reset+alloc the sched EVERY step
- L13413 — A cached cgraph is invalidated by ANY other graph on its scheduler — reset+alloc per step is NOT enough
- L13513 — TTS code predictor: the dispatch overhead is real, but "fuse into one graph" and "skip the reset" are BOTH wrong — the fix is a sched-free persistent graph
- L13582 — Any T-sized `ggml_view` into a fixed-length weight table must be bounded by the tensor's real length — long inputs overflow it and abort
- L13724 — gallocr does NOT protect input-flagged tensors across computes — re-set EVERY input before EVERY compute of a persistent graph
- L13785 — Vocoder/codec conv graphs hide three graph-construction wastes that dwarf the actual conv FLOPs — profile per-node before assuming it's inherent
- L13832 — Before optimizing a graph's *dispatch*, split host-encode vs GPU-execute — and never trust a GPU benchmark taken under load
- L13927 — A "GPU" model can be silently running on CPU — audit for hot graphs pinned to backend_cpu and CLI adapters that never forward use_gpu
- L17415 — PR #244 dequant-cache fix MERGED

**GPU portability — Metal / CUDA / Vulkan** (24)

- L1100 — When the full system needs an unavailable resource (model / GPU), factor the risky logic into a pure helper and prove IT on synthetic data
- L1215 — Verify a roadmap "broken/OPEN" claim empirically before implementing it — the codebase may have outgrown the note
- L1295 — A Metal masked-attention-padding penalty does NOT transfer to CUDA, and a bucket-width change is byte-identical on Metal/CPU but not on CUDA
- L1342 — ggml-metal im2col starves batch-1 convs — 3–11 threads/threadgroup at N=1, ~40× below bandwidth
- L2004 — A "garbled GPU output" bug can live entirely downstream — A/B the GPUs before localizing (§192)
- L2058 — MoltenVK `mul_mm`/`mul_mat_vec` downconvert src0 to f16 regardless of stored dtype (§192)
- L2069 — Vulkan has no `REPEAT f16→f16` — cast K/V to F32 *before* the GQA repeat (§192)
- L2082 — On macOS, GGML_METAL is default-ON and silently wins over Vulkan (§192)
- L2132 — §214 Metal batched (B=2) quantized mat-vec ≠ the single-token PREC_F32 path
- L2343 — Metal's q8_0 mat-vec kernel requantizes activations to q8 and ignores the F32 prec hint (§205)
- L2841 — Measuring GPU perf on a thermally-throttling laptop GPU
- L9086 — WDDM idle-clock-state hysteresis on consumer/laptop NVIDIA SKUs
- L9223 — Chatterbox #83 Round 9 — S3Gen UNet GPU drift on Metal
- L10476 — FA per-head additive mask CUDA kernel — what the upstream signature already gave us
- L11094 — funasr CUDA !-loop — all-NaN prefill logits (issue #125, §136)
- L12404 — Cohere flash-attn crossover
- L12464 — conv_transpose_1d GPU TDR — naive loop is O(IL), not O(K/s0)
- L12666 — flash_attn_ext vs scalar attention — numerical divergence
- L12777 — VibeVoice TTS garbles only on AMD RDNA4: coopmat2 flash-attention
- L13962 — Micro-optimizing an already-BLAS'd path with a GPU-ggml port is usually a dud — check for existing Accelerate/cblas and measure encode-vs-execute BEFORE porting
- L14285 — "Backend miscomputes my pipeline" ≠ "op X is broken": arbitrate with test-backend-ops, and beware aggregate precision (#304 native-Vulkan post-mortem)
- L16203 — Chatterbox GPU UNet performance fix
- L16442 — cosyvoice3 \"CUDA test FAIL\" was a registry-alias + flow-quant discovery bug, NOT a CUDA bug
- L17733 — "GPU picks a different token than CPU" is usually NOT a miscompute in an AR audio model — dump the LOGITS, and don't reach for the repetition detector

**Quantization** (10)

- L1559 — Auditing CrispASR against CrispEmbed's bug classes: a weight reader with no quantized branch fails silently (2026-07)
- L1639 — Quantizing a forced-aligner: q8 everywhere (incl. lm_head) is bit-identical; q4 on the encoder is not (#192 TADA aligner)
- L2689 — Orpheus/SNAC TTS: `token_embd` must stay F16 for sub-Q8 quants
- L3291 — Quantisation and memory
- L3566 — Quantization
- L12395 — F16 precision loss in deep WaveNet stacks
- L12412 — MeloTTS + BERT quantization — what works and what doesn't
- L13506 — Re-quantized GGUFs on HF must be re-baked when the quantizer adds carve-outs
- L13594 — Sub-8-bit quantization of an audio tower fails as BEHAVIORAL collapse (loops, empty output) — and there is no per-block cliff to bisect
- L17230 — imatrix quant SHIPPED

**Perf measurement & A/B discipline** (8)

- L1233 — Per-step matvec dispatch overhead is LOAD-DEPENDENT — a "win" measured on a busy box is mostly a contention artifact
- L1703 — Best-of-N only helps if the scorer measures what you care about — TADA's reconstruction scorer is blind to duration outliers (#192)
- L3327 — CPU vs ONNX vs PyTorch baselines
- L3770 — Performance: what faster-whisper / insanely-fast-whisper do
- L5067 — 2026-04-23 - FireRed decoder optimization triage
- L10775 — Distinguishing "slow run" from "hung run" — CPU time ≪ wall time is the signal
- L10910 — Cross-backend bug-sweep methodology — pair the cap survey with an empirical A/B
- L12848 — §176 runtime optimization audit methodology

**Long-form audio, VAD & chunking** (8)

- L1464 — LLM-ASR (SALM) instruction-echo on short windows is genuine model behaviour, not a port bug — gate the input, don't chase the prompt
- L1517 — The Mimi codec transformer must be causal — non-causal silently truncates long audio; the >250-frame WER A/B settled it (2026-07)
- L2910 — VAD + chunking
- L3843 — VAD integration and long audio
- L10599 — TDT single-pass over a full long utterance is numerically fragile to codec-level audio noise
- L14707 — #227 "--vad-import wants ggml-tiny" = LID not VAD
- L14929 — #300 streaming diarization SHIPPED
- L15525 — #89 parakeet-ja long-form FIXED

**Tokenizers, prompts, language & text** (17)

- L376 — MOSS-TTS-Local 4B stop runaway was a PROMPT-TOKENIZATION bug — non-compositional BPE, not the forward (#249, 2026-07)
- L426 — Cross-lingual TTS needs the target language plumbed through /v1/audio/speech (#249/#304, 2026-07)
- L1715 — A converter that embeds a tokenizer MUST embed the merges — a silent `try/except: pass` byte-fallback breaks everything downstream (#192)
- L3413 — Language handling
- L5234 — FireRedPunc / fullstop-punc — BERT punctuation restoration
- L6239 — Chatterbox 24 kHz prompt mel — module 4 phase 3
- L7398 — Text LID via fastText — GlotLID-V3 + LID-176
- L7609 — Text LID via CLD3 — Google compact language detector
- L12699 — VibeVoice TTS: missing BPE merges still need BPE behavior
- L13229 — 2026-07 — SentencePiece tokenizer taxonomy: greedy longest-match is wrong for BOTH Unigram and BPE
- L13486 — SentencePiece `byte_fallback` is not optional decoration — OOV emoji/symbols must become `<0xHH>` byte tokens, and a `utf8_aligned` Viterbi dead-ends at a multi-byte lead without it
- L13635 — The prompt contract is part of the port: a specials-only tokenizer silently sent EVERY glm-asr prompt instruction-less
- L13670 — Unigram run-length loop metrics pass 2-gram cycles — and a "raw" baseline is only raw if the disable gate actually exists
- L14843 — #249 MOSS-Local 4B stop-runaway = PROMPT tokenization bug
- L15222 — #329 TTS target language
- L16296 — cohere language whitelist + probe LID
- L16414 — CV3 phase 6 speech_tokenizer_v3 ggml port

**Mel, codecs & the audio front-end** (15)

- L1453 — A one-frame error in a codec ENCODER shifts every RoPE position and shows up as an onset artifact, not a gradual drift — and a padding scheme faithful to one codec can be wrong for another
- L2317 — A glibc-only crash reproduces on macOS under AddressSanitizer — and the bug is often a non-power-of-two FFT (§205)
- L2608 — Streaming conv modules need cached left context, not zero-padding (#81)
- L2786 — FFT size must match upstream exactly (Mini-Omni2 / Whisper mel)
- L3095 — mel / preprocessor
- L3243 — Mel spectrograms
- L3381 — Audio format lessons
- L4352 — Kyutai STT: causal padding, interleaved RoPE, and codec-based ASR
- L4486 — FireRedVAD: FSMN Conv1d replication
- L6287 — Chatterbox atomic native voice clone — the resampler + 5-cond install
- L11264 — moshi / Mimi RVQ codebooks: decode uses embed_sum / cluster_usage
- L12287 — MeloTTS (VITS2) — from zero to BERT conditioning
- L14804 — #245/§232 qwen3-tts CP_DIRECT + codec FASTCONV DONE
- L17129 — glint MP3+AAC TTS output §225 SHIPPED
- L17612 — voxcpm2 CausalTransposeConv1d kwargs gotcha

**Process, triage & documentation discipline** (18)

- L348 — When instrumenting the suspect file produces NO output, you are editing the wrong file — look for a second copy
- L441 — "The model emits it inline as text" is a claim to VERIFY, not to document — structured data you never parsed looks identical to a model limitation
- L698 — A positional arg landing on the WRONG parameter is invisible in review and baked into the weights
- L903 — PLAN "OPEN" items are frequently already shipped — audit against the CODE, never the prose
- L1141 — Audit a whole roadmap CLUSTER in one measure-first pass before implementing any of it
- L1447 — Handover docs can be dangerously stale — read the actual Python source, not the handover
- L2196 — DRY shared headers can exist for months before callers migrate (§175)
- L3489 — Regression testing discipline
- L3516 — Specific bugs that cost us a day each
- L3979 — CLI ↔ library DRY refactor
- L5543 — Audit script ≠ behavior test
- L10684 — Long-form ASR has three distinct failure classes, not one (2026-05-25, generalising issue #89)
- L12610 — beam_size default — greedy vs beam-5
- L12734 — VibeVoice TTS start clicks: distinguish decoder PCM from CLI post-processing
- L13282 — Issue #89 close-out: four transferable lessons
- L13577 — Issue triage discipline: check the codebase before leaving issues open
- L14204 — `git apply --3way` STAGES its result — a later `git add X && git commit` sweeps it up
- L16482 — CrispEmbed #31 WASM OCR e2e

**Per-model port notes** (43)

- L196 — A model's capability list cannot be inferred from its vocabulary — and forcing a fake list does not simulate having the capability
- L2801 — Multi-stream token architecture (Mini-Omni2)
- L3451 — Model architecture comparisons
- L4395 — FireRedASR: Conformer encoder debugging
- L5348 — TTS / Vocoder (Chatterbox HiFTGenerator)
- L5610 — Chatterbox-Turbo conformer encoder — ggml layout traps
- L5661 — Chatterbox repaired GGUF split — stage-specific regen
- L5836 — Chatterbox voice cloning — bake to GGUF, load via `--voice`
- L5889 — Chatterbox VoiceEncoder native port — module 2 of voice cloning
- L5995 — Chatterbox S3Tokenizer V2 native port — module 3 of voice cloning
- L6164 — Chatterbox CAMPPlus phase 2 — TDNN forward
- L7060 — T5-family translation runtime traps (May 2026, MADLAD-400 debugging)
- L7766 — IndexTTS-1.5 TTS backend
- L8233 — Speaker verification — TitaNet
- L8281 — Parakeet-TDT greedy decode — blank + duration=0
- L8347 — VibeVoice 1.5B TTS voice cloning: acoustic + semantic dual encoder
- L8830 — SANM-encoder family (FunASR / SenseVoice / CosyVoice)
- L11223 — Round 10 — SpeechT5 + Dia TTS backend ports (2026-05-31/06-01)
- L11298 — Dia 1.6B TTS — what ACTUALLY fixed it
- L11352 — FastPitch TTS — non-autoregressive parallel TTS port (§133, 2026-06-02/03)
- L11518 — SpeechT5 TTS decoder — what ACTUALLY fixed it
- L11592 — Parler TTS — T5 + MusicGen decoder + DAC 44 kHz
- L11724 — MAES beam search for TDT transducers
- L11826 — Bark TTS — what ACTUALLY fixed it
- L12373 — OpenVoice2 voice cloning — ggml data layout
- L13111 — TADA encoder port: staged model loading on constrained RAM (§221)
- L14425 — #195 parakeet-ctc-1.1b-ja CTC routing
- L14468 — #205 --max-len text-split + granite-plus
- L14585 — #221 irodori voice cloning SHIPPED
- L14748 — #231 cohere-arabic loop = corrupt GGUF
- L14861 — #249 MOSS voice cloning SHIPPED
- L14909 — vibevoice #299 runtime-reproduced
- L14974 — #308 whisper subtitles double-caps
- L15455 — #334 cosyvoice3 clone bugs
- L15593 — #93 voxtral-tts SHIPPED
- L16073 — ARK-ASR-3B port SHIPPED
- L16577 — CSM-1B TTS §135 FIXED
- L16621 — Dia 1.6B TTS port
- L16797 — dots.tts #200 SHIPPED
- L17170 — higgs-audio-v3-stt SHIPPED
- L17301 — indextts long-ref crash FIXED
- L17540 — TADA TTS time embedding bugs
- L17588 — Four bugs that together caused empty/garbage transcripts; all patched in src/vibevoice.cpp +…

## Cross-reference by model / family

A section is listed when its heading names the family, or its body mentions it
at least 3 times. Sections appear under several families; this is a
lookup, not a partition.

- **chatterbox** (25) — L2132, L2317, L5348, L5610, L5661, L5693, L5732, L5836, L5889, L5995, L6098, L6164, L6239, L6287, L8436, L9223, L12848, L13896, L14285, L14345, L14804, L15593, L16203, L16241, L16797
- **cosyvoice3** (9) — L516, L8830, L12848, L13896, L14345, L15222, L15455, L16414, L16442
- **vibevoice** (14) — L441, L1592, L2499, L5067, L5836, L8347, L12699, L12734, L12777, L13453, L14909, L14929, L15160, L17588
- **tada** (9) — L922, L1295, L1639, L1689, L1703, L12848, L13111, L15837, L17540
- **whisper** (22) — L116, L1808, L2786, L3586, L3770, L3843, L3979, L4486, L5427, L7398, L7766, L12610, L14468, L14974, L15017, L15083, L15525, L16073, L16414, L17170, L17230, L17346
- **parakeet / TDT** (16) — L649, L1808, L1841, L2910, L3843, L4189, L6287, L8281, L10599, L10684, L11724, L13282, L14425, L14468, L15525, L17346
- **moss** (13) — L376, L572, L1064, L1266, L1441, L1447, L1738, L1763, L1788, L2910, L14088, L14843, L14861
- **firered** (9) — L1233, L4395, L4486, L5067, L5234, L6098, L13229, L14974, L15017
- **funasr / sensevoice / SANM** (2) — L8830, L11094
- **kyutai / moshi / mimi** (7) — L1453, L1517, L4352, L11264, L12038, L12194, L16577
- **dia** (5) — L1195, L11223, L11298, L11518, L16621
- **parler** (1) — L11592
- **speecht5** (2) — L11223, L11518
- **bark** (1) — L11826
- **pocket-tts** (3) — L12038, L12194, L17415
- **melotts / VITS** (5) — L1359, L12287, L12412, L12827, L15895
- **fastpitch** (1) — L11352
- **indextts** (1) — L7766
- **orpheus / SNAC** (1) — L2689
- **voxcpm2** (4) — L8436, L8640, L8763, L17612
- **openvoice2** (1) — L12373
- **glm-asr** (1) — L13635
- **qwen3** (17) — L1266, L3140, L3843, L4486, L7165, L7766, L8436, L8830, L10812, L13594, L13692, L13785, L14468, L14804, L15222, L17230, L17733
- **madlad / T5** (1) — L7060
- **mini-omni2** (2) — L2786, L2801
- **titanet / speaker-id** (1) — L8233
- **audioseal** (1) — L12433
- **cohere** (7) — L196, L3243, L10748, L12404, L14468, L14748, L16296
- **voxtral** (11) — L3140, L3243, L3291, L3451, L3843, L10684, L10748, L10775, L10910, L15593, L16797
- **granite** (3) — L1883, L3140, L14468
- **ark-asr** (2) — L16073, L16797
- **higgs-stt** (2) — L572, L17170
- **dots-tts** (2) — L1447, L16797
- **irodori** (2) — L1324, L14585
- **f5-tts** (1) — L13927
- **kokoro** (4) — L289, L7165, L15222, L15895
- **zonos** (1) — L11592
- **omniasr / omnivoice** (6) — L116, L1019, L4486, L13724, L13752, L14138
- **glm / mimo / mega** (3) — L10940, L13635, L13692
